<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "dev.mvc.fgenre.FgenreDAOInter">


<!-- 등록  -->
<insert id="create" parameterType="FgenreVO">
  INSERT INTO fgenre(fgenreno, genreno, filmno)
  VALUES (fgenre_seq.nextval #{genreno}, #{filmno})
</insert>

<!-- 목록 -->
<select id="list" resultType="FgenreVO">
  SELECT fgenreno, genreno, filmno
  FROM fgenre
  ORDER BY filmno DESC, fgenreno ASC
</select>

<!-- 조회 -->
<select id="read" resultType="FgenreVO" parameterType="int">
  SELECT fgenreno, genreno, filmno
  FROM fgenre
  WHERE fgenreno = #{fgenreno}
</select>

<!-- 수정 -->
<update id="update" parameterType="HashMap" >
  UPDATE fgenre
  SET genreno = #{genreno}, filmno = #{filmno}, 
  WHERE fgenreno = #{fgenreno}
</update>

<!-- 삭제 -->
<delete id="delete" parameterType="int">
  DELETE 
  FROM fgenre 
  WHERE fgenreno=#{fgenreno}
</delete>


</mapper>